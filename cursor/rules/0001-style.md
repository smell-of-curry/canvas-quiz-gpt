## Canvas Quiz GPT Style Guide

- Prefer early returns over nested conditionals to keep control flow flat and readable.
- Document all exported JavaScript and TypeScript symbols (functions, classes, types, constants) with JSDoc/TSDoc comments so intent stays clear.

